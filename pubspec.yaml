name: myQuran
description: A new Flutter project.

publish_to: "none" # Remove this if you plan to publish

version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.2.0
  url_launcher: ^6.2.6
  audioplayers: ^5.2.1
  share_plus: ^10.0.2   # 🔹 add this
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/bg.jpg
    - assets/bg.png
    - assets/images/splash.gif
    - assets/images/developer.jpg
    - assets/sounds/bell-chime.mp3
    - assets/surah_info.json

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/logo_512.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/icons/logo_256.png"



